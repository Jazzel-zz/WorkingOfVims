@model IEnumerable<VIMS.Models.Estimate>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EstimateNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ApplicationUser.Name)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.VehicleInformation.VehicleName)
        </th>
        <th>
            Policy @Html.DisplayNameFor(model => model.PolicyType.Type)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(model => item.EstimateNumber)
            </td>
            <td>
                @Html.DisplayFor(model => item.ApplicationUser.Name)
            </td>
            <td>
                @Html.DisplayFor(model => item.VehicleInformation.VehicleName)
            </td>
            <td>
                @Html.DisplayFor(model => item.PolicyType.Type)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
        
    }

</table>
    